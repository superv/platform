<?php

namespace Tests\{addon.short_name};

class {addon.short_name}Test extends TestCase
{
    function test_addon_is_installed()
    {
        $this->assertNotNull(superv('addons')->get('{addon.slug}'));
    }
}
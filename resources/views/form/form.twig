{% extends "port::layouts/default" %}

{% block content %}
    <div class="col-md-12">
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title"></h3>

                <div class="pull-right box-tools">
                    {% for button in form.buttons %}
                                             {{ button.render()|raw }}
                                         {% endfor %}
                    {#<button type="button" class="btn btn-info btn-sm" data-toggle="tooltip"#}
                            {#title="" data-original-title="Remove">#}
                        {#<i class="fa fa-times"></i> Provision Package#}
                    {#</button>#}
                </div>
            </div>

            <div class="box-body">
                {{ form(response) }}
                {#{{ form_start(form) }}#}
                {#{{ form_errors(form) }}#}
                {#{{ form_widget(form) }}#}
                {#{{ form_end(form) }}#}
            </div>
        </div>
    </div>

{% endblock %}